# Feature Layer / Слой фич

## English

User actions live here. A feature may import from `entities` and `shared`. Direct imports between features are not allowed.

## Русский

Здесь находятся пользовательские действия. Фича может импортировать код из `entities` и `shared`. Прямые импорты между фичами запрещены.
